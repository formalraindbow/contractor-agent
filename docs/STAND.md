# Стенд для проверки агента

## Актуальные ветки

`develop` — рабочая версия, `main` — версия для общего тестирования.
После обновления 05.09.2026 обе содержат один и тот же код и финальные материалы команды.
Дальнейшие изменения делаем в `develop` и переносим в `main` после проверки.

Старая `prompt-v2` включена в историю. Экспериментальная реализация сохранена тегом
`archive/review-v4-2026-09-05`; выбранные изменения интерфейса уже перенесены в текущую версию.
Ветки сравнения и их старый скрипт запуска удалены. Исторические результаты в
`deliverables/interim/` относятся к указанным в них версиям.

## Локальный запуск

```sh
uv sync --frozen
# Один раз: скопируйте .env.example в .env и задайте ключ провайдера и модель.
uv run kontragent-api --host 127.0.0.1 --port 8080
```

В `.env` задаются `LLM_BASE_URL`, `LLM_MODEL`, ключ провайдера и `WEB_PASSWORD`.
Пароль обязателен для общего стенда. Он используется существующим входом по ссылке
с параметром `k` или HTTP Basic с логином `alfa`.
Ключи модели и пароль не коммитятся. `/v1/health` после входа показывает выбранную модель.

В актуальном общем стенде используется GPT-OSS-120B через Яндекс — та же модель,
на которой проверяли последнюю версию на порту 8084; запасные модели отключены,
reasoning=low, лимит генерации 8192 токена. Настройки задаются окружением процесса.

Диалоги публичного стенда: `.cache/sessions.sqlite`. Существующий файл сохраняется
при перезапуске. Локальная проверка на 8084 использует отдельный
`.cache/review-develop.sqlite`. Не удаляйте эти файлы при обновлении кода.

## Доступ коллег через Pinggy

После запуска приложения, в отдельном терминале:

```sh
ssh -o StrictHostKeyChecking=accept-new -o ServerAliveInterval=30 \
  -o ExitOnForwardFailure=yes -p 443 -R0:localhost:8080 qr@a.pinggy.io
```

Используйте HTTPS-адрес из вывода текущего процесса и добавьте ключ доступа `k`.
Бесплатный туннель действует 60 минут; новый запуск выдаёт новый адрес.
Запуск приложения и SSH должны оставаться активными, компьютер — включённым.
Проверяйте вход, получение карточки и один ответ агента через внешний адрес.
Актуальные адреса на машине стенда записываются в игнорируемый Git файл `runs/links.txt`.

## Проверки перед обновлением

```sh
uv run ruff check .
uv run pytest -q
```

Проверки интерфейса: `scripts/review_ui_smoke.cjs` и
`scripts/review_goal_ui_smoke.cjs` (Playwright, локальный стенд 8084).
Протокол предыдущих проверок: [REVIEW_STANDS_IMPLEMENTATION.md](REVIEW_STANDS_IMPLEMENTATION.md).
