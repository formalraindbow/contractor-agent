# Обновление демонстрационного стенда 7 сентября

В 12:11 МСК на https://kontragent-135-106-194-178.sslip.io/ развёрнута сборка
`8cc84c6a65b4c9656fa139698a0f6ad914fd573e` из `codex/v8-release-clean`.
Образ: `kontragent:selectel-v8-8cc84c6`.

Агент: `openai/gpt-oss-20b` через Polza. Предпочтение провайдеров — Groq,
AkashML; разрешена маршрутизация к другим провайдерам той же модели.
Переключение на 120B не настроено. Настройка порядка не подтверждает, какой
провайдер фактически исполнил конкретный запрос.

Перед переключением сохранены конфигурация, исходники и согласованная резервная
копия SQLite. Старый образ `kontragent:selectel-v7-d089785` сохранён для отката.
Рабочая база чатов осталась на прежнем диске. Копия базы, использованная при
проверке staging, не заменяла рабочую базу.

## Проверка после обновления

- Контейнер healthy, публичный API сообщает `openai/gpt-oss-20b`.
- Контрольный существующий чат: 8 записей истории; SHA-256 состояния до и после
  одинаков: `99c58e4e2cc97e88a1cd8f2f7239ef150547ca19d6062f684fef30f6502b03e9`.
- HTML публичного интерфейса совпал с локально сформированным HTML этой сборки.
- Восемь HTTP/SSE-запросов завершились без ошибок транспорта, с правильным
  порядком событий, совпадением потока с итогом и без невалидных ссылок.
  Проверены карточка, прямая рекомендация, объяснение блокировки, выбор компании,
  уточнение об одной компании после сравнения, посторонний запрос, неизвестный
  ИНН и отсутствие прибыли. Наблюдавшееся время — 0,05–8,68 с.
- Все восемь ответов прочитаны вручную. Один текстовый assert smoke не распознал
  корректную фразу «а не что компания понесла убыток». Исходный неуспешный
  результат сохранён, а ручная проверка записана отдельно; это не изменение
  метрики качества модели.
- Перед выпуском: 501 локальный тест прошёл; целевые живые проверки судов,
  прямого отрицательного ответа и отрицательных чистых активов завершились
  содержательным ответом.

Локальные артефакты (не публикуются вместе с данными отчётов):
`runs/v8-polza/production-8cc84c6-http.json`,
`production-8cc84c6-verification.json`, `production-before-v8.json`,
`production-after-v8.json`. На сервере происхождение сборки и путь резервной
копии записаны в `/opt/contractor-agent/active_release.json`.

## Ограничения результатов

Полный прогон 219 сценариев на момент выпуска ещё работает на зафиксированной
более ранней сборке b8f9152. Он не перезапускался и не подменялся после
обновления стенда. Завершённые повторы и оценки разных версий перечислены
в REPEATS_AND_CALIBRATION.md. Демонстрационная проверка не означает завершение
полной приёмки или готовность к банковскому промышленному внедрению.
