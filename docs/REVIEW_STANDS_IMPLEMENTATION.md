# Доработки develop по отзыву от 05.09.2026

Основа — `develop` на `ab823a2`. Сохранены движок сигналов, три исхода рекомендации,
карточка отдельно от уточнений, пять целей и сравнение по трём группам.
Перенесено светлое оформление с боковой навигацией и панелями отчёта/оснований.
Ветка `codex/review-v4` целиком не сливалась.

## Что изменено

| Пункт отзыва | Результат |
|---|---|
| 1–2. Служебные поля и баннеры | Имена полей остаются в раскрытых основаниях; черновики, шаги инструментов и счётчики не выводятся в чат. Ошибки структурирования не превращаются в ответ. |
| 3. Объяснение и документы | Объяснение остаётся разговорным; список документов строится по фактам конкретной компании. Нет автоматического запроса сведений о штате. |
| 4. Платёж и отсрочка | Ответ про зелёную метку отделён от решения о платеже. По ГДК показаны блокировка, иски, капитал и убыток. По отсрочке — финансы по годам, ограничения и документы. |
| 5. Три исхода | Формулировки и правила develop сохранены. Цель меняет порядок фактов. |
| 6. Финансы | Выручка, прибыль или отсутствие строки, капитал и резервы, ликвидность или отсутствие исходных данных. Для устаревшей отчётности сохраняется пояснение о сроке. |
| 7. Отсутствие записей | В сводках судов/приставов используются «в отчёте не найдено» и граница данных, без нулевых сумм вместо пропусков. |
| 8. Уточнения | Новый ответ собирается по текущим данным, без старых черновиков. В истории модели сохраняются проверенные ответы; руководитель и возраст не перерисовывают карточку. |
| 9. Запись демо | [Видео](../deliverables/interim/develop-review/demo.webm), [дословный сценарий](DEMO_SCRIPT.md), три локальных прохода. |
| 10. Цели | Переставляют классы сигналов, подсказки и запросы документов; вердикт не меняют. Цели расположены перед длинной карточкой. |
| 11. Основания | Под карточкой, уточнениями и сравнением. Исходные поля, значения и расчёты раскрываются отдельно. Есть сводка полного отчёта. |
| 12. Качество | Три колонки и выбор сохранённого прогона; понятные названия; ошибки показаны отдельно. Исторические результаты не выданы за проверку новой сборки. Старые длительности с временем судьи скрыты. |
| 13. MCP | «8 функций», переносимый конфиг; явно указан запуск по stdio из установленного проекта. Публичный MCP-адрес не заявлен. |
| 14. Интерфейс | Русские кавычки, дата в шапке карточки один раз, компактные ответы, Enter/Shift+Enter, сохранение набираемого текста, прокрутка и мобильная ширина. |

## Как устроены ответы

Новая инструкция разделяет выбор инструментов и подготовку ответа. Свободные пояснения,
обычные финансовые вопросы и сравнение по-прежнему формулирует модель.
Точные сводки судов и приставов, пояснение меток, документы, решение об отсрочке и отказ
при отсутствии численности формируются из типизированных данных. Это предотвращает
повторение наблюдавшихся ошибок: неверных сумм, придуманной методики ЗСК, утверждения
об отсутствии блокировок в ответе только о судебном деле и выдуманного статуса дела.
Ни один из этих блоков не меняет правила рекомендации.

Разбивка судебных дел за год не сообщает их статус и стороны спора. Общая сводка
ЛЕ МОНЛИД показывает и 1525 дел, и расхождение с 1488 распределёнными по ролям.
Определение ЗСК проверено по [Банку России](https://www.cbr.ru/counteraction_m_ter/platform_zsk).

Исправлен и технический сбой: пустой результат JSON-структурирования теперь запускает
резервный способ структурирования той же моделью. Непроверенный черновик не показывается.
Числовой валидатор учитывает точность округления вместо безусловного допуска в 5%.

## Проверки

- `uv run ruff check .` — успешно.
- `uv run pytest -q` — 247 тестов успешно, включая сохранение сессии после закрытия
  и повторного создания runtime, корректные ссылки и границы отсутствующих данных.
- Браузер: отправка Enter, перенос Shift+Enter, ввод во время ответа, история после F5,
  автопрокрутка, порядок фактов без смены вердикта, три группы сравнения, панели отчёта
  и оснований, переход из отчёта к основаниям, MCP, качество, ширина 390 px — успешно;
  ошибок JavaScript не обнаружено. Проверки взаимодействий используют контролируемый
  ответ для теста ввода; демо ниже использует настоящую модель.
- **14/14 реальных запросов** по замечаниям завершились, отклонённых цитат в итогах нет.
  Медиана 3.50 с, диапазон 0.99–25.91 с,
  без судьи качества. [Ответы и метаданные](../deliverables/interim/develop-review/acceptance.json).
- **3/3 прохода демо** с новыми профилями Chrome, без ошибок JavaScript;
  [прогоны](../deliverables/interim/develop-review/demo-runs.json).

Проверена GPT-OSS-120B в Яндексе — та же модель, которую возвращают прежние стенды
8081 и 8082. Запасные модели отключены, reasoning=low, лимит генерации 8192 токена.
Кандидат 20B показал ошибки и не был принят как замена. Эти проверки не являются
новым полным прогоном `evals/gold.yaml` с независимым судьёй и не доказывают качество
на произвольных вопросах. Для свободных пояснений проверка одних цитат также
не гарантирует правильность всех смысловых выводов.

## Запуск и хранение

Локальный стенд: http://127.0.0.1:8084/.
Рабочая копия: `/Users/alantolparov/IdeaProjects/contractor-agent-develop`. Из неё: `uv sync --frozen`, затем `uv run kontragent-api`.
Локальный `.env` настроен и исключён из Git. Ключи в коммиты не входят.

Память диалога хранится в SQLite по `SESSION_DB_PATH`, браузер хранит список проверок,
цель и показанные ответы. В Compose файл сессий вынесен в именованный том `chat-sessions`.
Это конфигурация одного экземпляра сервиса; при переносе в Kubernetes нужен постоянный
том для этого пути. Общая база для нескольких реплик и синхронизация списка чатов
между устройствами в эту доработку не входят.

Сценарий **с чужого устройства/внешней сети не проверен**. Перед защитой его нужно
проверить на опубликованном адресе; локальная запись служит запасным вариантом.
